<li class="chat-item {{#if active}}chat-item--active{{/if}}">
  <div class="chat-item__avatar">
    <div class="avatar">
      <span class="avatar__placeholder">{{initials}}</span>
    </div>
  </div>
  <div class="chat-item__body">
    <div class="chat-item__header">
      <span class="chat-item__name">{{name}}</span>
      <span class="chat-item__time">{{time}}</span>
    </div>
    <div class="chat-item__footer">
      <span class="chat-item__message">{{lastMessage}}</span>
      {{#if unreadCount}}
      <span class="chat-item__badge">{{unreadCount}}</span>
      {{/if}}
    </div>
  </div>
</li>
